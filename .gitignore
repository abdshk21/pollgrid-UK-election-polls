# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Poll data and derived artifacts are never committed, except crosstabs.yaml
# — it's hand-curated source data, not a fetched artifact (see CLAUDE.md).
data/*
!data/crosstabs.yaml
